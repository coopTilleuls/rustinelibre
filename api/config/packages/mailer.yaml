framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        envelope:
            sender: 'Rustine Libre <%env(MAILER_SENDER)%>'
        headers:
            From: 'Rustine Libre <%env(MAILER_SENDER)%>'

when@test:
    framework:
        mailer:
            dsn: 'null://null'
