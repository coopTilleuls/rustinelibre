{% extends 'mail/mail_layout.html.twig' %}

{% block body_mail %}
<div style="padding:20px; text-align:center">
  <h1 style="font-weight:700; font-size:28px; margin-bottom:30px">Nouvelle demande de RDV</h1>
  <p style="margin-bottom:14px; font-size:14px;">{{ firstName }} {{ lastName }}</p>
  <p style="margin-bottom:30px; font-size:14px;">{{ appointment.slotTime | date('d/m/Y H:i') }}</p>
  <div style="text-align:center; margin-bottom:30px;">
    <a href="{{ appointmentUrl }}" style="color:#fff;text-decoration:none;">
      <div style="display:inline-block; width:fit-content; background-color:#ee721d; padding-top:6px; padding-bottom:6px; padding-left:24px; padding-right:24px; border-radius:30px; font-size:14px">Voir détails du RDV</div>
    </a>
  </div>
</div>
{% endblock %}
