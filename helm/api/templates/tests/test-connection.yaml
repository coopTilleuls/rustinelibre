apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bikelib-api.fullname" . }}-test-connection"
  labels:
    {{- include "bikelib-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bikelib-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
