apiVersion: v1
kind: Service
metadata:
  name: {{ include "bikelib.fullname" . }}
  labels:
    {{- include "bikelib.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bikelib.selectorLabels" . | nindent 4 }}
