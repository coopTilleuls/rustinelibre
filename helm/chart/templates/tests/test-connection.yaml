apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bikelib.fullname" . }}-test-connection"
  labels:
    {{- include "bikelib.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bikelib.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
